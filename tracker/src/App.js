import TestButton from './TestButton';

function App() {
  return (
    <div>
      <TestButton initCont={0} text="dasd"/>
      <TestButton initCont={0} text="dasd"/>
      <TestButton initCont={0} text="dasd"/>
      <TestButton text="dasd"/>
    </div>
  );
}

export default App;
